export function calculateDayStats(items){const total=items.length,completed=items.filter(i=>i.status==='COMPLETED').length,missed=items.filter(i=>i.status==='MISSED').length,pending=items.filter(i=>i.status==='PENDING').length;const completionRate=total?Math.round(completed/total*100):0,missedRate=total?missed/total:0;const dayStatus=total===0?'NEUTRAL':(missed===0&&completed===total?'GREEN':(missedRate<.5?'YELLOW':'RED'));return {total,completed,missed,pending,completionRate,missedRate,dayStatus}}
